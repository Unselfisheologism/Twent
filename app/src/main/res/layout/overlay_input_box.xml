<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlayRootLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <EditText
        android:id="@+id/overlayInputField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/input_box_background"
        android:hint="@string/input_box_hint"
        android:imeOptions="actionDone"
        android:inputType="text"
        android:padding="16dp"
        android:textColor="@android:color/white"
        android:textColorHint="#A0A0A0" />

    <!-- Attach file button row -->
    <LinearLayout
        android:id="@+id/attachButtonRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="8dp">

        <!-- Attach button (paperclip icon) -->
        <ImageButton
            android:id="@+id/attachButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_attach"
            android:background="@drawable/control_button_background"
            android:contentDescription="Attach file or screen"
            android:scaleType="centerInside"
            android:padding="8dp" />

        <!-- Optional: Text label -->
        <TextView
            android:id="@+id/attachButtonText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap to attach"
            android:textColor="#A0A0A0"
            android:textSize="12sp"
            android:layout_marginStart="12dp" />

    </LinearLayout>

</LinearLayout>